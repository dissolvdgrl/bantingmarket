<div class="card p-4">
    <p class="text-sm">{{ $titleSmall }}</p>
    <h2 class="font-serif uppercase font-bold text-xl">{{ $title }}</h2>
    <div class="stripe bg-black mb-8"></div>
    <p>{{ $text }}</p>
    <a href="{{ $link }}" class="link mt-8" target="{{ $target }}">{{ $linkText }}</a>
</div>